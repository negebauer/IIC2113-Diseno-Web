# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.4
    working_directory: ~/web
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "4b:58:23:a4:98:12:15:bb:60:39:34:c0:db:e3:d1:4f"
      - restore_cache:
          keys:
          - dependency-cache-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - dependency-cache-

      - run: sudo npm i -g yarn
      - run: yarn
      # - run: bash .circleci/setup-api.sh $CIRCLE_BRANCH

      - save_cache:
          paths:
            - node_modules
          key: dependency-cache-{{ checksum "package.json" }}

      - run: yarn test -- --coverage
      - run: yarn lint
      - run: yarn build
      - store_test_results:
          path: ./coverage
      - store_artifacts:
          path: ./coverage
          destination: coverage
  deploy:
    docker:
      - image: circleci/node:8.4
    working_directory: ~/web
    steps:
      - checkout
      - run: wget -qO- https://cli-assets.heroku.com/install-ubuntu.sh | sh
      - run: bash .circleci/setup-heroku.sh $CIRCLE_BRANCH
      - run: bash .circleci/setup-env.sh $CIRCLE_BRANCH
      - add_ssh_keys:
          fingerprints:
            - "48:a0:87:54:ca:75:32:12:c6:9e:a2:77:a4:7a:08:a4"
      - deploy:
          name: Deploy to Heroku
          command: git push heroku $CIRCLE_BRANCH:master

workflows:
  version: 2
  workflow:
      jobs:
        - build
        - deploy:
            requires:
              - build
            filters:
              branches:
                only:
                  - master
                  - dev
